import React, { useEffect } from 'react';
import App from './App';
import eventBus from "../../shared/eventBus";

const Root = (props) => {
  
  return (
    <section>
      <App />
    </section>
  );
};

export default Root;
